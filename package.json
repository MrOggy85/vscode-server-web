{
  "private": true,
  "description": "Pinned npm packages installed globally inside the container image.",
  "dependencies": {
    "@anthropic-ai/claude-code": "latest",
    "typescript": "latest"
  }
}
